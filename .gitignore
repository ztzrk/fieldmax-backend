# Node.js & Dependencies
# Dependencies are installed via `pnpm install`, so we don't need to track them.
/node_modules

# Environment Variables
# This file contains secrets like database passwords and API keys. NEVER commit this.
.env
.env.*
!.env.example

# TypeScript Build Output
# This is the compiled JavaScript code. It's generated from your .ts files.
/dist
*.tsbuildinfo

# Prisma
# The Prisma client is generated automatically. The default location is covered
# by node_modules, but if you have a custom output path, list it here.
# Your entry of /src/generated/prisma is correct for a custom output path.

# Logs & Reports
# Log files generated during runtime.
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test reports
coverage
.nyc_output

# OS & Editor Specific
# Files generated by macOS, Windows, and code editors.
.DS_Store
Thumbs.db
.vscode/
.idea/